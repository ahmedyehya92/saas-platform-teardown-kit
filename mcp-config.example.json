{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"]
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "<optional, raises rate limits for public repo research>"
      }
    }
  },
  "_notes": [
    "playwright: required for the live web-platform audit (Phase 2). Runs headed by default so you can watch it explore the app.",
    "github: optional, only useful when the target SaaS has public repos (open-source desktop shell, SDKs, public issue tracker/changelog).",
    "Confirm exact package names/versions at install time — MCP server packages update independently of this kit."
  ]
}
