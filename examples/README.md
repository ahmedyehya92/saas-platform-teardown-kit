# Example teardowns

Generated by running the finished kit — the files below are committed as
the runs produced them (reports self-date via their headers).

**Point-in-time research snapshots.** SaaS products ship weekly — treat
every specific here as dated. The methodology is the durable artifact;
the facts are demonstrations of it.

| Example | Product / domain | Researched | Kit state | Engine · tier | Invocation |
|---|---|---|---|---|---|
| [Linear](linear-teardown/00-INDEX.md) | linear.app — project tracking, issues, cycles | 2026-09-07 (01:07–01:43 UTC) | `03f6d8a` (v1.0.0) | Engine A (built-in web search/fetch) · Tier 1 (Playwright MCP live audit) | `Do a full platform teardown of https://linear.app` |

## Linear — run notes

- **Coverage:** web + iOS + Android + macOS + Windows desktop + public
  GraphQL API/webhooks + 72-listing integration directory + changelog;
  hardware = verified none (checked where, stated in the report).
- **Access:** disposable-identity signup on the free tier (email-only,
  no payment info, per the operating rules); live workspace walked
  end-to-end via Playwright MCP; store listings and revenue estimators
  via search + fetch.
- **Claims:** 267 confidence-tagged claims — 119 `Confirmed` / 61
  `Reported` / 31 `Inferred` (every inference shows its reasoning).
- **Effort:** ~36 minutes wall clock, ~141 tool calls (14 web searches,
  14 page fetches, ~63 browser actions).
- `recon-dossier.md` is the per-fact working file the run appended to as
  it went — kept raw to show the kit's trail from sweep to report.
